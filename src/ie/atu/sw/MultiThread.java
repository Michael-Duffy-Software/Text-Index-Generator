package ie.atu.sw;
import java.util.ArrayList;
import java.util.concurrent.Executors;

public class MultiThread {

	/*
	public static ArrayList multiThreadbuildWordsPerPageArray(String pageText, ArrayList commonWordsArray) throws Exception {

		MultiThread.go();
		ArrayList wordsPerPageArray = new ArrayList();
		return wordsPerPageArray;

	}

	public static void go() throws Exception {
		try (var pool = Executors.newVirtualThreadPerTaskExecutor()) {

			for (int i = 0; i < 10; i++) {
				int j = i;
				pool.execute(() -> process(j));

			}

		}
	}

	public static void process(int i) {
		System.out.println("PROCESSING : " + i);
	}
	
	*/

}